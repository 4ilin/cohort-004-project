# Database Schema

ER diagram of the SQLite schema defined in [`app/db/schema.ts`](../app/db/schema.ts) (Drizzle ORM).

```mermaid
erDiagram
    users {
        int id PK
        text name
        text email UK
        text role "student | instructor | admin"
        text avatar_url
        text bio
        text created_at
    }

    categories {
        int id PK
        text name
        text slug UK
    }

    courses {
        int id PK
        text title
        text slug UK
        text description
        text sales_copy
        int instructor_id FK
        int category_id FK
        text status "draft | published | archived"
        text cover_image_url
        int price "cents"
        bool ppp_enabled
        text created_at
        text updated_at
    }

    modules {
        int id PK
        int course_id FK
        text title
        int position
        text created_at
    }

    lessons {
        int id PK
        int module_id FK
        text title
        text content
        text video_url
        text github_repo_url
        int position
        int duration_minutes
        text created_at
    }

    enrollments {
        int id PK
        int user_id FK
        int course_id FK
        text enrolled_at
        text completed_at
    }

    lesson_progress {
        int id PK
        int user_id FK
        int lesson_id FK
        text status "not_started | in_progress | completed"
        text completed_at
    }

    quizzes {
        int id PK
        int lesson_id FK
        text title
        real passing_score
    }

    quiz_questions {
        int id PK
        int quiz_id FK
        text question_text
        text question_type "multiple_choice | true_false"
        int position
    }

    quiz_options {
        int id PK
        int question_id FK
        text option_text
        bool is_correct
    }

    quiz_attempts {
        int id PK
        int user_id FK
        int quiz_id FK
        real score
        bool passed
        text attempted_at
    }

    quiz_answers {
        int id PK
        int attempt_id FK
        int question_id FK
        int selected_option_id FK
    }

    purchases {
        int id PK
        int user_id FK
        int course_id FK
        int price_paid "cents, post-PPP"
        text country "PPP geo-lock"
        text created_at
    }

    teams {
        int id PK
        text created_at
    }

    team_members {
        int id PK
        int team_id FK
        int user_id FK
        text role "admin | member"
        text created_at
    }

    coupons {
        int id PK
        int team_id FK
        int course_id FK
        text code UK
        int purchase_id FK
        int redeemed_by_user_id FK
        text redeemed_at
        text created_at
    }

    course_ratings {
        int id PK
        int user_id FK
        int course_id FK
        int rating "1-5"
        text created_at
        text updated_at
    }

    video_watch_events {
        int id PK
        int user_id FK
        int lesson_id FK
        text event_type
        real position_seconds
        text created_at
    }

    %% Content hierarchy
    users ||--o{ courses : "teaches (instructor_id)"
    categories ||--o{ courses : categorizes
    courses ||--o{ modules : contains
    modules ||--o{ lessons : contains

    %% Learning / progress
    users ||--o{ enrollments : enrolls
    courses ||--o{ enrollments : "enrolled in"
    users ||--o{ lesson_progress : tracks
    lessons ||--o{ lesson_progress : "progress on"
    users ||--o{ video_watch_events : emits
    lessons ||--o{ video_watch_events : "watched in"
    users ||--o{ course_ratings : rates
    courses ||--o{ course_ratings : "rated as"

    %% Quizzes
    lessons ||--o{ quizzes : has
    quizzes ||--o{ quiz_questions : contains
    quiz_questions ||--o{ quiz_options : offers
    users ||--o{ quiz_attempts : attempts
    quizzes ||--o{ quiz_attempts : "attempted as"
    quiz_attempts ||--o{ quiz_answers : records
    quiz_questions ||--o{ quiz_answers : "answered by"
    quiz_options ||--o{ quiz_answers : "selected as"

    %% Commerce
    users ||--o{ purchases : makes
    courses ||--o{ purchases : "purchased as"
    teams ||--o{ team_members : has
    users ||--o{ team_members : "member of"
    teams ||--o{ coupons : owns
    courses ||--o{ coupons : "redeemable for"
    purchases ||--o{ coupons : "generated by"
    users o|--o{ coupons : "redeems (nullable)"
```

## Notes

- Enum values (`role`, `status`, `question_type`) are enforced at the TypeScript level only — stored as plain `text` with no DB CHECK constraints.
- Timestamps are ISO-8601 strings in `text` columns, defaulted app-side via Drizzle `$defaultFn`.
- Money (`price`, `price_paid`) is stored as integer cents; quiz scores are `real`.
- `coupons.redeemed_by_user_id` is the only nullable FK (unredeemed coupons).
- Junction-like tables (`enrollments`, `lesson_progress`, `team_members`) have no composite unique constraints — duplicate prevention happens in the service layer. `course_ratings` is the exception: a unique index on `(user_id, course_id)` enforces one rating per student per course at the DB level.